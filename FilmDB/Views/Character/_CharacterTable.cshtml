@using FilmDB.Models.Database
@model List<Character>
<div class="table-responsive">
	<table class="table table-striped table-hover">
		<thead style="position: sticky; top: 0; background: white; z-index: 100; box-shadow: 0px 2px 5px rgba(0,0,0,0.1);">
			<th class="w-70">Name</th>
			<th class="w-30">?</th>
		</thead>
		<tbody>
			@foreach (var Character in Model)
			{
				<tr>
					<td>
						<a asp-controller="Character" asp-action="CharacterDetail" asp-route-characterId="@Character.CharacterId">
							@Character.Name
						</a>
					</td>
					<td>
						?
					</td>					
				</tr>
			}
		</tbody>
	</table>
</div>