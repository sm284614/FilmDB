@model Collaboration

<div class="container">
	<div>
		<h1 class="d-inline">@Model.Person.Name Collaberations</h1>
	</div>
	<div>
		<table class="table table-striped table-hover">
			<thead>
				<tr>
					<th>Film Title</th>
					<th>Year</th>
					<th>Job Title</th>
				</tr>
			</thead>
			<tbody>
				@foreach (var collaboration in Model.CollaborationList)
				{
					<tr>
						<td>
							<a asp-controller="Person" asp-action="PersonFilmography" asp-route-id="@collaboration.Person.PersonId">
								@collaboration.Person.Name
							</a>
						</td>
						<td>@collaboration.Job.Title</td>
						<td>@collaboration.JobCount</td>
					</tr>
				}
			</tbody>
		</table>
	</div>
</div>
